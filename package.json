{
  "name": "kairos-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Kairos - Time-centered productivity system",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "clean": "turbo run clean && rm -rf node_modules",
    "backend:dev": "cd apps/backend && ./mvnw spring-boot:run",
    "backend:build": "cd apps/backend && ./mvnw clean package",
    "frontend:dev": "cd apps/frontend && npm run dev",
    "frontend:build": "cd apps/frontend && npm run build",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "docker:logs": "docker-compose logs -f"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/felipemelozx/kairos.git"
  },
  "author": "Felipe Melo",
  "license": "MIT"
}
