meta {
  name: Logout
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/auth/logout
  body: none
  auth: none
}

headers {
  Content-Type: application/json
}

docs {
  # Logout
  
  Clears authentication cookies (ACCESS_TOKEN and REFRESH_TOKEN).
  
  Requires valid authentication cookies from login/register.
}
